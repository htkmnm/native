import React from 'react';
import { View, Button, TextInput } from 'react-native';

const Hello = (props) => {
    return (
        <View>
            <Button title='press' />

        </View>
    );
}

export default Hello;